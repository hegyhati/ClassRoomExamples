# Notes

 - `class`
 - `__init__`
 - `x` vs `_x` vs `__x`
 - methods & state
 - `__str__`
 - composition


# Exercises

1. `Fraction` class, implement:
   - `Fraction(nominator,denominator)`
   - `f + f`
   - `f - f`
   - `f * f`
   - `f / f`
   - `f ** int`
   - `__str__`
2. `Point2D` class, `distanceFrom`, `getX`, `getY`
3. `Polyline` class, `add(Point)`, `totalLength`, `toSVG`, `minX`, `maxX`, `minY`, `maxY`
