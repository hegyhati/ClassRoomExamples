# Software Engineering
2026.05.20.

### 1. Erklär den Unterschied zwischen Validierung und Verifikation. Was bedeutet es, wenn die Verifikation passt, aber die Validierung fehlschlägt? Bringen Sie ein Beispiel. 

### 2. Erkläre ein wichtiges agiles Konzept in ein bis zwei Sätzen.

### 3. Definier die folgenden Konzepte in einem Satz 
  - YAGNI 
  - DRY

### 4. Wahr/Falsch

 - `git add` fügt einen neuen Commit zum aktuellen Branch hinzu.
 - Iterative und inkrementelle Entwicklung sind dasselbe.

### 5. Was ist technische Schuld (technical debt)? Erkläre sie in zwei bis drei Sätzen.

### 6. Erzähl in eigenen Worten, was ein Unit-Test ist und warum wir ihn verwenden.

### 7. Wir haben die folgende Klasse, die eine Lösung modelliert:

```py
class Solution:
    def __init__(self, water_volume:float, solvent_volume:float, solvent_name:str) -> None: ...
    def volume_density(self) -> float: ...
    def add_water(self,volume:float) -> None: ...
    def same_solvent(self, other_solution:"Solution") -> None: ...
```

Welche Unit-Tests können für diese Klasse implementiert werden? (Mind. 3)

### 8. Nimm an, du hast eine funktionierende Software und der Kunde hat eine Idee für ein neues Feature – etwas wirklich Einfaches. Nenne die wichtigsten Schritte, die in Scrum / auf GitHub passieren, bis das neue Feature ausgeliefert wird. Einfach geordnete Schritte, zum Beispiel: "Das Team schätzt die Komplexität der Aufgabe" oder "Der Entwickler implementiert das Feature" oder "Der lokale Branch wird zu GitHub gepusht".

